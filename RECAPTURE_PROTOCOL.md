# 재촬영 캡처 프로토콜 (체크리스트)

목적: 세 문제(① 등방 스케일 −2.4%, ② 윗면 마커 z, ③ 파지 T_G_O)를 **해결 또는 귀속 가능**하게 만드는 데이터 확보.
설정: 카메라 4대 (현재 고정 = cam0/1/3, 그리퍼 = cam2), AprilTag 59mm 큐브.

핵심 원칙: **−2.4%는 큐브 재촬영으로 안 고쳐진다.** vision 탓인지 robot 탓인지 *측정 가능*하게 만드는 게 목표(→ §1, §2). 큐브 재촬영은 ②③ 전용(§3, §4).

---

## §0. 촬영 전 물리 실측 (촬영과 무관, 먼저 못박기)

- [ ] 옆면 마커 검은 사각 변: 4개 각각 캘리퍼 실측 → `marker_size_by_id` (현재 51mm 확정)
- [ ] 윗면 마커 검은 사각 변: 2개 실측 (현재 25mm 가정)
- [x] **윗면 마커 면 확정 (②): 탭 상단 = +29.5mm.** 마커 2개가 그리퍼가 잡는 돌출 탭의 가장 윗면에 부착됨.
      → `config.CubeConfig.marker_center_m` ID 0·1 z = +0.0295 **맞음, 수정 불필요.** (데이터의 top-vs-side <1mm와도 일치)
- [ ] 큐브 한 변 실측 → `cube_side_m` (현재 0.059)
- [ ] charuco 사각 변 실측 → intrinsics 캘리브의 `square_length_m` (현재 0.025 가정)
- [ ] 그리퍼가 잡는 탭 위치·폭 실측 (31mm 폭, z=57~59 가정) → §4 T_G_O 초기값

---

## §1. depth 활성화 ★최우선 (−2.4% 귀속의 핵심)

이번 세션은 depth PNG는 저장됐지만 `depth_valid=False`가 전부였음. 원인: `apriltag_cube.py`의 depth 샘플링이 stub(`{"valid": False}` 하드코딩)이었기 때문. **→ 수정 완료**: `_depth_plane_metrics()` 구현으로 측정 depth vs PnP 평면 depth를 비교해 depth_* 필드를 채운다. 기존 데이터 재처리 시 626/627(100%)가 유효해짐. 캡처 코드(`build_frame_record`)는 이미 depth를 넘기고 있었으므로 **재촬영은 자동으로 depth가 채워진다.**

- [ ] 캡처 후 확인: side 마커/`cube_pnp`의 `depth_z_median_mm`·`depth_inlier_ratio`·`depth_plane_median_mm` 채워지는지
- [ ] **plane_median_mm 이 작은지(수 mm) 확인** — 현재 기존 데이터는 ~37mm로 큼. 원인: 단일마커 IPPE pose 오차 + 인쇄 태그의 IR 반사 불량(종이 태그 위 depth 신뢰도 낮음). → §4a 다면관측으로 pose 개선 필요.
- [ ] depth 유효 조건: 마커 영역 depth 샘플 ≥ 20, inlier_ratio 높게
- [ ] 큐브를 depth 유효거리(RealSense 통상 0.3~2m) 안에 배치
- [ ] 스케일 판별은 태그면이 아니라 **매트한 큐브 본체/기준면**의 depth로 하는 게 신뢰도 높음(태그 IR dropout 회피)

판별 로직 (재촬영 후):
- PnP거리 ≠ depth거리 (~2.4% 차) → **vision/intrinsics 문제 (A)**
- PnP거리 = depth거리, 근데 FK와 2.4% 차 → **robot/FK 스케일 문제 (B)**

---

## §2. 독립 길이 기준물 (스케일 교차검증)

- [ ] 캘리퍼로 실측한 게이지블록/세라믹 자 등 **인증 길이 물체**를 고정캠 시야에 포함해 몇 프레임 촬영
- [ ] 또는 charuco 보드를 고정캠이 정면+tilt로 보게 별도 촬영 (보드 = 알려진 metric 물체)
- [ ] 기준물은 마커·로봇과 독립 → vision 절대 스케일을 직접 검증

---

## §3. intrinsics(charuco) 재촬영 ★−2.4%가 vision이면 여기서 해결

현재 intrinsics: 카메라당 16~17뷰, "저장 이미지 오프라인 재캘리브", factory 대비 초점거리 균일 −2%. tilt 다양성 부족이 초점거리를 못 박지 못한 정황.

- [ ] 카메라당 **30~50뷰**
- [ ] 보드가 프레임의 **네 구석·중앙**을 모두 커버 (렌즈 왜곡 구속)
- [ ] **강한 tilt: pitch/yaw ±30~45°** 다양하게 (초점거리를 구속하는 핵심 — fronto-parallel만 찍으면 f 편향)
- [ ] 여러 거리(가까이~멀리)
- [ ] 각 카메라 개별 캘리브, RMS < 0.3px 목표, 사용 뷰 수 기록
- [ ] `square_length_m` = §0 실측값
- [ ] 결과 판정:
      - 새 f ≈ 593(recal) 재현 → intrinsics는 recal이 맞음 → −2.4%는 **robot(B)**
      - 새 f ≈ 607(factory)이나 또 다른 값 → intrinsics가 원인(A), 새 값 채택

---

## §4. 큐브 재촬영 (②③ 전용, −2.4%와 무관)

현재 데이터의 약점(측정치):
- 단일 마커 PnP 위주 → 큐브 pose 깊이 불확실성 큼 (멀티마커 ≥2 = 고정 19 + 그리퍼 26건뿐)
- **옆면 2개 동시관측 = 0건**
- 윗면 마커 고정캠 커버리지 붕괴 (set 3~12: 0~3회, set 9 = 0회)

### 4a. 다면 동시관측 확보 (가장 큰 품질 개선)
- [ ] 각 고정캠이 **top + 옆면 2개**를 동시에 보도록 큐브/카메라 배치 (큐브 모서리를 카메라로, 방위·고도 ~45°)
- [ ] 이미지당 **동시 가시 마커 ≥ 3** 목표
- [ ] "옆면 2개 동시" 프레임을 다수 확보 (현재 0 → 수십 건)

### 4b. set별 전 면 균형 커버리지
- [ ] set마다 큐브 회전을 바꿔 6개 면이 고정캠에 고르게 잡히게
- [ ] **set당 윗면(ID 0·1) 고정캠 관측 ≥ 10회** (현재 set 9 = 0)
- [ ] set당 각 옆면 고정캠 관측 ≥ 10회

### 4c. 파지 T_G_O 관측 (③, 현재 identity placeholder)
- [ ] 큐브를 **쥔 채** 팔 자세를 넓게 변화 (다양한 T_B_G), 각 자세에서 고정캠이 큐브를 보게
- [ ] 파지 자세 ≥ 20개, 회전·위치 모두 다양하게 (T_G_O 관측성 확보)
- [ ] 재촬영 후 `T_B_O = T_B_G @ T_G_O` 를 전 자세로 풀어 T_G_O 추정 → `experiment_config.GraspConfig.T_G_O` 기입, `is_placeholder=False`
- [ ] 파지중심은 §0 실측(탭 z=57~59면 frame +28.5)으로 초기값, 반변 29.5 근사 금지

---

## §5. 재촬영 후 검증 (돌려서 확인할 것)

- [ ] depth vs PnP 거리 스케일 테스트 → A/B 판별 (§1)
- [ ] 독립 기준물 스케일 검증 (§2)
- [ ] top-vs-side 큐브중심 계통오차(큐브프레임 z) 재측정 → ② 확정 (이번엔 다면관측으로 노이즈↓)
- [ ] per-marker 관측 수·재투영 오차 재확인 (§4b 커버리지 달성 여부)
- [ ] CP_C1 재실행 후 cons_t / grip_t / down_mm 개선 확인

---

## 우선순위 요약

| 순위 | 항목 | 해결/판별 대상 |
|---|---|---|
| ★1 | depth 활성화 (§1) | −2.4% vision/robot 귀속 (A vs B) |
| ★2 | 독립 길이 기준물 (§2) | vision 절대 스케일 검증 |
| 3 | intrinsics 고품질 재촬영 (§3) | A일 때 −2.4% 해결 |
| 4 | 큐브 다면·균형·파지 촬영 (§4) | ② z, ③ T_G_O, pose 품질 |
| 0 | 물리 실측 (§0) | 전 항목 추측 제거 |
