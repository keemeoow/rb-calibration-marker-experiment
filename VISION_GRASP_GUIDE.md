# 카메라로 본 물체를 로봇이 잡기까지

카메라가 물체를 찾아주면, 그 값을 로봇 명령으로 바꿔야 한다. 이 문서는 그
사이에 무엇을 곱하고 무엇을 더해야 하는지를 정리한다.

- 어떤 행렬 파일을 쓰는가
- 어떤 오프셋을 더하는가
- 물체 크기 정보가 왜 필요한가

마지막에 실제로 계산해 본 예제(노란 블록)를 붙였다.

---

## 1. 전체 흐름

카메라가 주는 값에서 로봇 명령까지 네 단계다.

```
① 카메라가 본 물체        T_cam_obj      (비전이 출력)
② 로봇 베이스 기준 물체    T_base_obj = T_base_C1 @ T_cam_obj
③ 잡을 지점               물체 중심 + 물체 크기로 계산
④ 로봇에 보낼 명령         ③ + 그리퍼 길이
```

②에서 **캘리브레이션 행렬**을 쓰고, ③에서 **물체 크기**를 쓰고,
④에서 **그리퍼 오프셋**을 쓴다. 셋은 서로 다른 것을 채우므로 섞으면 안 된다.

| 단계 | 채우는 것 | 물체가 바뀌면 |
|---|---|---|
| ② 캘리브레이션 행렬 | 카메라와 로봇의 관계 | 안 바뀜 |
| ③ 물체 크기 | 중심에서 잡을 지점까지 | **바뀜** |
| ④ 그리퍼 오프셋 | 플랜지에서 손가락 끝까지 | 안 바뀜 |

---

## 2. 어떤 파일을 쓰는가

### 2.1 외부파라미터 — 카메라를 로봇 베이스에 연결

경로는 `data/session04/calib_out/` 이다.

| 파일 | 뜻 | 언제 쓰나 |
|---|---|---|
| `T_base_C0.npy` | 베이스 ← cam0 | cam0 영상으로 물체를 볼 때 |
| **`T_base_C1.npy`** | **베이스 ← cam1 (D415)** | **cam1 영상으로 물체를 볼 때** |
| `T_base_C3.npy` | 베이스 ← cam3 | cam3 영상으로 물체를 볼 때 |
| `T_gripper_cam.npy` | 플랜지 ← 그리퍼 카메라 | 손에 달린 카메라로 볼 때 |
| `T_base_O.npy` | 베이스 ← 마커 큐브 | 캘리브레이션 검증용. 잡기에는 안 씀 |

전부 4×4 행렬이고 **길이 단위가 미터**다. 로봇은 mm를 쓰므로 마지막에 1000을 곱한다.

`T_base_C2.npy`는 없다. cam2는 이번 캘리브레이션에 안 들어갔다.

> **주의** — 물체를 본 카메라와 같은 번호의 행렬을 써야 한다. cam1 영상에
> `T_base_C0.npy`를 곱하면 완전히 엉뚱한 곳이 나온다.

### 2.2 내부파라미터 — 카메라 렌즈 특성

경로는 `intrinsics/` 이고, 비전이 직접 PnP를 풀 때 필요하다.

| 파일 | 시리얼 | 해상도 |
|---|---|---|
| `cam0.npz` | 314522062542 | 1280×720 |
| **`cam1.npz`** | **319522062138 (D415)** | **1280×720** |
| `cam2.npz` | 752112070297 | 1280×720 |
| `cam3.npz` | 912322060991 | 1280×720 |

안에 `color_K`(3×3), `color_D`(왜곡계수), `serial`, `color_w`, `color_h`가 들어 있다.

**해상도를 반드시 맞춰야 한다.** 1280×720으로 캘리브레이션했으므로 다른 해상도로
촬영하면 `color_K`가 안 맞는다.

### 2.3 calib_out 과 calib_out_nofk

두 벌이 있다. FK(로봇 관절값)를 쓴 결과와 안 쓴 결과다. 성능은 사실상 같다.

| 지표 | calib_out | calib_out_nofk |
|---|---|---|
| 카메라 간 일치 (중앙값) | 2.08mm | 1.97mm |
| 카메라 간 일치 (평균) | 2.28mm | 2.24mm |
| hand-eye 산포 | 2.78mm | 2.94mm |

**기본은 `calib_out`을 쓴다.** 차이가 측정 오차 수준이라 어느 쪽이 낫다고
말할 근거가 아직 없다. 실물로 비교해 본 적도 없다.

---

## 3. 오프셋 — 무엇을 얼마나 더하는가

### 3.1 캘리브레이션은 플랜지 기준이다

`T_base_C1`은 **플랜지**(tool 오프셋 0) 자세로 풀었다. 그래서 이 행렬이 주는 것도
플랜지 기준 좌표다. 여기에 그리퍼 길이를 더해야 손가락이 어디 있는지 나온다.

### 3.2 쓸 값은 115.5mm 하나다

```
TOOL_FINGERTIP_Z = 115.5   # 플랜지 → 손가락 끝
```

`server/c1.py:53`에 정의돼 있고, 교시 파일 `place_tcp`를 역산하면 114.99가 나와
독립적으로 확인된다.

코드에 나오는 다른 두 값은 이 용도로 쓰면 안 된다.

| 값 | 정체 | 왜 안 되는가 |
|---|---|---|
| `TOOL_GRIPPER_Z = 150.0` | 컨트롤러 모션 규약 | 기구학 근거가 없다. 34.5mm 어긋난다 |
| `TOOL_CUBE_CENTER_Z = 143.0` | 플랜지 → 마커 큐브 중심 | **마커 큐브 반높이가 섞여 있다.** 다른 물체엔 안 맞는다 |

150은 티칭 이후에 컨트롤러 설정이 바뀌면서 남은 값이다. 교시 파일의 `place_tcp` z가
30.62인데 코드의 `PLACE_TCP_Z_MM`이 −5.2인 것이 그 흔적이다(차이 35.8mm ≈ 34.5mm).

### 3.3 settool 을 어떻게 잡든 물리적 위치는 같다

**툴 값은 로봇이 가는 자리를 바꾸지 않는다.** 같은 자리를 몇으로 부를지만 바꾼다.

| settool | 명령할 TCP z | 실제 플랜지 z |
|---|---|---|
| 115.5 | −17.60 | 97.90 |
| 150 | −52.10 | 97.90 |

두 줄 모두 그리퍼가 똑같은 곳에 간다. **다만 툴을 바꾸면 기존 교시값이 전부
그만큼 어긋나므로**, 기존 촬영 경로와 같은 프로그램에서 쓴다면 툴은 그대로 두고
명령값만 환산하는 쪽이 안전하다.

새 프로그램을 따로 짠다면 **settool 115.5가 맞다.** 그러면 "TCP를 잡을 지점으로
보내라"가 그대로 성립해서 중간 계산이 사라진다.

---

## 4. 물체 크기가 왜 필요한가

### 4.1 비전은 중심을 주고, 손가락은 중심에 가면 안 된다

비전이 주는 `T_base_obj`의 원점은 보통 **물체 중심**이다. 그런데 손가락은 중심이
아니라 **물체 옆면을 물 수 있는 높이**에 가야 한다. 그 차이가 물체 크기에서 나온다.

```
                 ┌─────────┐  ← 윗면
                 │         │
   손가락 끝 ────→│    ●    │  ← 비전이 주는 중심
                 │         │
                 └─────────┘  ← 아랫면
                 ═══════════   테이블
```

손가락은 **끝보다 위쪽에서 물체와 닿는다.** 그래서 손끝을 깊이 넣을수록 닿는 면이
넓어지고 덜 미끄러진다. 대신 너무 내리면 손가락이 테이블을 친다.

높이를 모르면 이 둘 중 어느 쪽에 걸리는지 알 수 없다.

### 4.2 위아래 한계가 크기로 정해진다

높이 h인 물체가 테이블에 놓여 있을 때,

```
윗면    = 중심 z + h/2
아랫면  = 중심 z - h/2   (= 테이블 면)
```

손끝은 이 사이에 있어야 한다. **윗면보다 위면 헛잡고, 아랫면보다 아래면 테이블을
친다.** 30mm 블록이면 폭이 30mm뿐이라 여유가 거의 없다.

### 4.3 권장 지점은 중심

특별한 이유가 없으면 **손끝을 물체 중심 높이에** 둔다. 위쪽 절반 전체가 물리고,
테이블까지 h/2 만큼 여유가 남는다.

마커 큐브(59mm)에 쓰던 "윗면 아래 2mm"는 그 큐브 전용 값이다. 30mm 블록에 쓰면
2mm만 걸쳐서 미끄러진다.

### 4.4 회전에도 크기(모양)가 필요하다

물체 단면이 정사각형이면 90도 단위로 어느 방향이든 잡히지만, 직사각형이면
**짧은 변을 물어야** 한다. 그리퍼 벌림 폭보다 긴 변은 애초에 안 들어간다.

---

## 5. 계산 절차

### 5.1 로봇 자세 표기 규약

이 로봇의 6자유도 값은 **`[x, y, z, Rz, Ry, Rx]`** 순서다. 네 번째가 Z축 회전이고
여섯 번째가 X축 회전이며, 회전은 `R = Rz @ Ry @ Rx`로 조립된다. 길이는 mm, 각도는 도.

session04 촬영 91건의 기록값과 대조해 확인했다(최대 오차 1e-6도).

### 5.2 코드

```python
import numpy as np

FINGERTIP_Z = 115.5          # mm, 플랜지 → 손가락 끝

T_base_C1 = np.load("data/session04/calib_out/T_base_C1.npy")   # 미터 단위

# ① 비전이 준 카메라 기준 물체 자세 → 4x4 (미터)
T_cam_obj = ...              # 회전 규약은 비전 쪽 출력에 맞춘다

# ② 베이스 기준으로 옮긴다
T_base_obj = T_base_C1 @ T_cam_obj
cx, cy, cz = T_base_obj[:3, 3] * 1000.0        # mm

# ③ 물체의 어느 축이 위를 향하는지 찾는다
R = T_base_obj[:3, :3]
up = int(np.argmax(np.abs(R[2, :])))           # z 성분이 가장 큰 열
horiz = [i for i in range(3) if i != up]

# ④ 잡을 방향: 수평축 하나의 방위각에 그리퍼를 맞춘다
u = R[:, horiz[0]]
azim = np.degrees(np.arctan2(u[1], u[0]))
cands = np.array([azim + 90 * k for k in range(-3, 4)])   # 정사각 단면이면 90도
rz_target = cands[np.argmin(np.abs(cands - current_rz))]  # 현재에서 가장 가까운 것

# ⑤ 잡을 높이: 손끝을 물체 중심에
flange_z = cz + FINGERTIP_Z

target = [cx, cy, flange_z, rz_target, 0.0, 180.0]   # 위에서 수직 접근
```

`Ry=0, Rx=180`은 그리퍼가 정확히 아래를 보게 하는 값이다. 이때 플랜지 X축의
베이스 방위각이 그대로 `Rz`가 되므로 ④의 계산이 성립한다.

단면이 직사각형이면 ④의 `90`을 `180`으로 바꾼다.

### 5.3 움직이는 순서

**한 번에 대각선으로 내려가면 안 된다.** 두 단계로 나눈다.

| 단계 | 하는 일 |
|---|---|
| 1 | 현재 높이 그대로 xy와 Rz만 맞춘다 |
| 2 | 눈으로 확인한 뒤 수직으로만 하강 |

---

## 6. 예제 — 노란 블록 (30mm)

### 입력

```
현재 플랜지   -253.48, 474.21, 148.84, Rz -115.00, Ry 0.00, Rx 180.00
FoundationPose 출력 (cam1 기준)
  위치  x -35.7   y 30.0   z 608.9  mm
  자세  rx -28.7  ry -55.5  rz -151.8  도   (extrinsic XYZ)
블록 높이 30mm
```

### ② 베이스로 옮기기

```
T_base_obj = T_base_C1 @ T_cam_obj

위치 (mm)   x -304.38   y 445.50   z -17.60
```

물체 축이 베이스에서 향하는 방향을 보면 자세를 알 수 있다.

| 메시 축 | 베이스 방향 | 수직과의 각 |
|---|---|---|
| X | +Y | 85.8° |
| **Y** | **위** | **4.6°** |
| Z | +X | 88.0° |

**메시 Y축이 위**를 가리킨다. 수평축들은 베이스 X·Y와 1° 이내로 정렬돼 있어
블록이 반듯하게 놓여 있다.

### ③ 크기 반영

```
윗면   = -17.60 + 15 = -2.60
아랫면 = -17.60 - 15 = -32.60
손끝 목표 = 중심 = -17.60
```

### ④ 명령값

```
목표 플랜지  (-304.38, 445.50, 97.90, Rz -89.34, Ry 0, Rx 180)

현재 대비    dx  -50.90    dy  -28.71    dz  -50.94    dRz  +25.66
```

`Rz`는 블록 수평축 방위각 0.66°에서 90을 뺀 −89.34다. 현재 −115에서 25.66도만
돌면 되어 가장 가깝다.

settool 115.5로 명령한다면 TCP z는 **−17.60**(블록 중심 높이 그대로)이다.

---

## 7. 검증 — 내려가기 전에 확인할 것

### 7.1 테이블 높이와 맞는지

테이블 면은 base z **−30.57mm**로 추정된다. 마커 큐브(59mm) 윗면이 28.43mm임을
실물로 확인해 역산한 값이다.

```
물체 아랫면 = 중심 z - 높이/2
```

이 값이 −30.57과 몇 mm 안에 들어오면 좌표가 맞은 것이다.

노란 블록 예제에서는 −32.60이 나와 **2.03mm 차이**였다. 통과.

**10mm 넘게 벗어나면 내려가지 말 것.** 카메라 번호를 잘못 골랐거나 회전 규약이
안 맞을 가능성이 높다.

### 7.2 캘리브레이션이 낼 수 있는 정확도

`verification_metrics.json` 기준이다.

| 항목 | 값 |
|---|---|
| 카메라 간 일치 (평균) | 2.28mm |
| 자세 반복성 | 2.33mm |
| 치수 정확도 | 1.99mm |
| 메시 정합 RMSE | 3.89mm |

**대략 2~4mm로 보면 된다.** 여기에 비전 자체의 오차가 더해진다. 그리퍼 여유가
이보다 작은 작업은 이 값만으로 성공하지 못한다.

---

## 8. 알려진 주의사항

### 8.1 마커 한 장만 보이면 깊이가 크게 틀어진다

AprilTag 큐브를 마커 하나(id 4)로만 재봤더니 base z가 **17mm** 어긋났다. 그 면이
카메라에서 66도나 기울어 있던 것이 원인으로 보인다. 연속 프레임 사이에서도 깊이가
2mm씩 흔들렸다.

**최소 두 면이 보이게** 물체를 놓거나 카메라 각도를 바꾸는 것이 좋다.

같은 조건에서 FoundationPose 결과는 테이블 높이와 2mm 안에서 맞았다. 이 오차는
캘리브레이션이 아니라 마커 한 장 PnP의 문제일 가능성이 크다.

### 8.2 여러 장을 찍어 중앙값을 쓴다

한 장으로 판단하지 않는다. `live_marker_pose.py`는 `--frames`로 여러 장을 찍어
중앙값을 쓰고 표준편차를 함께 출력한다. **z 표준편차가 2mm를 넘으면** 그 측정은
믿지 않는 편이 낫다.

### 8.3 툴 오프셋을 두 번 더하지 않기

캘리브레이션 기록은 플랜지(tool1, 오프셋 0)로 남겼다. 그리퍼 길이는 hand-eye에
흡수돼 있지 않으므로 잡을 때 한 번 더해야 하지만, **컨트롤러가 이미 툴 오프셋을
적용 중이면 두 번 더해진다.** 어느 툴로 명령하는지 확인하고 한 번만 반영한다.

### 8.4 남은 확인거리

- `calib_out`과 `calib_out_nofk` 중 어느 쪽이 실제로 나은지 실물 비교
- 테이블 높이 −30.57mm는 추정치다. 직접 재보면 더 확실하다
- 마커 큐브 17mm 오차의 원인이 정말 단일 마커 때문인지

---

## 부록 — 참조 위치

| 내용 | 위치 |
|---|---|
| **이 문서의 계산을 그대로 하는 스크립트** | **`grasp_target.py`** |
| 그리퍼 오프셋 정의 | `server/c1.py:42-57` |
| 그립/놓기 높이 | `server/c1.py:69-72` |
| 로봇 자세 규약 구현 | `live_marker_pose.py:50-78` |
| 실시간 측정 스크립트 | `live_marker_pose.py` |
| 캘리브레이션 방식 설명 | `CALIBRATION_EXPLANATION_LATEX.md` |

`grasp_target.py`는 numpy만 있으면 돈다. 5장의 계산과 7장의 검산을 그대로 한다.

```bash
python3 grasp_target.py \
    --obj "-35.7 30.0 608.9 -28.7 -55.5 -151.8" --cam cam1 \
    --size 30 \
    --flange "-253.48 474.21 148.84 -115.00 0.00 180.00"
```

주요 옵션이다.

| 옵션 | 뜻 | 기본값 |
|---|---|---|
| `--cam` | 물체 자세의 기준 좌표계. `cam0/cam1/cam3/base` | `cam1` |
| `--euler` | 물체 회전의 오일러 순서. `--intrinsic` 로 내재 지정 | `xyz` 외재 |
| `--size` | `"높이"` 또는 `"가로 세로 높이"` | 필수 |
| `--grip_depth` | 윗면에서 얼마나 아래를 물지 | 높이/2 (중심) |
| `--symmetry` | 단면 회전 대칭. 정사각 90, 직사각 180 | `90` |
| `--tool` | 명령에 쓸 settool 값 | `115.5` |
| `--table` | 검산에 쓸 테이블 높이 | `-30.57` |

테이블 검산에 실패하면 종료코드 2를 낸다. 자동화할 때 여기서 멈추면 된다.

`live_marker_pose.py`는 AprilTag 큐브 전용이고 pyrealsense2가 설치된 환경에서만 돈다.

```bash
/home/sstone/anaconda3/envs/ur3-eye-to-hand/bin/python live_marker_pose.py \
    --frames 15 --flange "-253.48 474.21 148.84 -115.00 0.00 180.00"
```
