# 비교 방법 6개, 아주 쉬운 설명 (대학 1학년용)

## 0. 애초에 무슨 문제를 푸는 건가

로봇과 카메라는 **서로 다른 "자기 기준"** 으로 세상을 잰다.

- 로봇: "내 발바닥(base)에서 오른쪽으로 30cm, 앞으로 20cm"
- 카메라: "내 렌즈에서 정면으로 50cm, 왼쪽으로 10cm"

카메라가 "컵이 여기 있어!"라고 알려줘도, 로봇은 그게 **자기 기준으로 어디인지** 모르면 집을 수 없다.
그래서 필요한 게 **변환표(캘리브레이션)**: "카메라 기준 좌표 → 로봇 기준 좌표"로 번역해주는 표.
이 변환표를 얼마나 정확하게 알아내느냐가 이 논문 전체의 주제다.

**모든 방법이 쓰는 공통 재료 두 가지:**

1. 로봇은 **자기 손이 어디 있는지 항상 안다.** 모터마다 각도 센서가 있어서, 각도들을 팔 길이와
   조합하면 손끝 위치가 계산된다. 이 계산을 **FK(순기구학)** 라고 부른다.
2. 카메라는 **마커(체스판 무늬, QR코드 비슷한 태그)를 보면** 그 마커가 카메라에서 얼마나
   떨어져 어떤 각도로 있는지 계산할 수 있다. (마커의 실제 크기를 아니까, 사진에 찍힌
   크기·모양의 찌그러짐으로 역산 — 이게 PnP다.)

즉 "로봇이 아는 것(FK)"과 "카메라가 본 것(마커)"을 **연결**해서 변환표를 알아낸다.
방법들의 차이는 **이 연결을 어떤 수학으로, 무엇을 믿고 푸느냐**다.

---

## 1. Tsai–Lenz (1989) — 원조. "회전 먼저, 이동은 나중에"

**셀카봉 비유.** 손에 셀카봉을 들고 폰을 붙였다고 하자. 셀카봉이 어떻게 생겼는지(길이·각도)
모른다. 그런데 손을 이리저리 움직이면 — 손이 움직인 궤적은 내가 알고(로봇 FK),
폰이 본 장면이 어떻게 변했는지는 폰이 안다(카메라). **같은 움직임을 두 시점에서 본 셈**이라,
이 둘을 비교하면 "손과 폰 사이의 관계(셀카봉의 생김새)"를 역산할 수 있다.
이게 그 유명한 **AX=XB** 문제다. (A=손의 움직임, B=카메라가 느낀 움직임, X=손→카메라 변환 = 구하려는 것)

Tsai의 요령은 **문제를 반으로 쪼개는 것**: ① 먼저 "어느 방향으로 돌아붙었나"(회전)만 풀고,
② 그 답을 이용해 "얼마나 떨어져 붙었나"(이동)를 푼다. 두 번의 쉬운 계산으로 끝나서 빠르다.

- **약점**: ①에서 조금이라도 틀리면 그 오차가 ②로 **그대로 흘러들어간다.** 첫 단추를 잘못
  끼우면 끝까지 어긋나는 구조.
- **우리 표에서의 역할**: "제일 기본적인 방식은 이 정도 나온다"는 출발선.

## 2. Daniilidis (1999) — "회전과 이동을 한 번에"

Tsai의 약점(오차 전염)을 없앤 방법. **듀얼 쿼터니언**이라는 수학 도구를 쓰는데, 겁먹을 것 없다 —
핵심만 말하면 **회전과 이동을 하나의 숫자 묶음으로 포장해서, 쪼개지 않고 한 방에 푸는 것**이다.

시험 비유: 두 문제가 이어져 있을 때, 1번 답을 2번에 대입하는 방식(Tsai)은 1번을 틀리면 2번도
자동으로 틀린다. Daniilidis는 두 문제를 **연립해서 동시에** 푼다. 그래서 관측에 노이즈가 있어도
Tsai보다 잘 버틴다. "공식 한 방" 계열 중에서는 가장 성능이 좋다고 알려져 있다.

- **우리 표에서의 역할**: 고전 방법의 "베스트". 이것보다 나아야 새 방법의 의미가 있다.

## 3. Shah (2013) — "카메라 위치와 보드 위치, 미지수 2개를 한 방에"

앞의 두 방법은 미지수가 1개(X)였다. 그런데 실제로는 모르는 게 하나 더 있다:
**마커(보드)가 로봇 기준으로 어디 있는지**도 모른다. 미지수 2개짜리 문제 — **AX=ZB**
(X=카메라 변환, Z=보드 위치, 둘 다 미지수)라고 부른다.

Shah는 행렬을 길게 펴서 늘어놓는 트릭(Kronecker 곱)으로, 이 2-미지수 문제를
**"공식에 대입하면 답이 바로 나오는" 형태(닫힌 해, closed-form)** 로 만들었다.
반복 계산 없이 한 번에 X와 Z가 동시에 나온다.

여기서 중요한 특징 하나: 이 계열은 **로봇 FK를 100% 믿는다.** FK가 알려주는 손 위치를
"정답"으로 놓고 나머지를 끼워 맞춘다. FK가 정확하면 좋지만, FK에 오차가 있으면
그 오차가 고스란히 답에 박힌다.

- **우리 표에서의 역할**: "FK를 통째로 믿는" 진영의 표준 공식.

## 4. Tabb & Ahmad Yousef (2017) — "공식 한 방 대신, 사진에 대고 조금씩 고치기"

앞의 세 방법은 전부 "공식 한 방"이다. Tabb의 통찰은 이것: **오차가 실제로 태어나는 곳은
사진의 픽셀이다.** 마커 코너를 사진에서 찾을 때 1~2픽셀씩 틀리는 게 오차의 근원인데,
공식 한 방 방법들은 픽셀이 아니라 어떤 인공적인 수식 거리를 최소화한다. 재는 자가 다른 것이다.

그래서 Tabb은 이렇게 한다:

1. 공식 한 방(Shah 같은 것)으로 **대충의 답**을 얻는다.
2. 그 답이 맞다고 치고, "그럼 마커 코너가 사진의 **어느 픽셀**에 찍혀야 하지?"를 계산해서
   사진 위에 겹쳐 그려본다. (이걸 **재투영**이라 한다)
3. 예측 위치와 실제 찍힌 위치가 몇 픽셀 어긋났는지 재고, 어긋남이 줄어드는 방향으로
   답을 **조금 수정**한다.
4. 2~3을 어긋남이 더 안 줄어들 때까지 **반복**한다.

퍼즐 조각을 공식으로 계산해 한 번에 놓는 게 아니라, 놓아보고 → 어긋난 만큼 밀고 →
다시 놓아보는 것. 실제 데이터에서는 이 방식이 공식 한 방보다 꾸준히 정확하다.
(참고: 우리 방법도 "픽셀 단위로 재서 반복 수정"이라는 이 철학 위에 서 있다.)

- **우리 표에서의 역할**: "픽셀 기준 + 반복 수정" 진영의 고전 대표. 단, FK는 여전히 100% 믿는다.

## 5. Allegro 외 (RA-L 2024) — "카메라 여러 대를 한꺼번에, 서로 검증시키며"

지금까지는 전부 **카메라 1대**짜리 방법이었다(여러 대면 한 대씩 따로 돌림).
그런데 카메라를 따로따로 캘리브하면 각자 자기 오차를 안고 끝난다 — 서로 확인해줄 방법이 없다.

Allegro의 방법(우리의 최근접 경쟁자):

- 보드를 **로봇 손에 붙이고**, 로봇이 보드를 여러 위치로 움직여준다. 카메라 4대가 다 같이 본다.
- 그리고 **모든 카메라를 한 판에 놓고 동시에** 최적화한다. 조건은 두 가지:
  ① 각 카메라의 답은 로봇 FK와 맞아야 하고, ② **카메라끼리 서로 본 것도 맞아야 한다**
  (카메라 1과 카메라 2가 같은 순간 같은 보드를 봤으면, 두 답이 모순되면 안 된다).
- ②가 핵심이다. 여러 명이 같은 것을 재고 **서로 교차 검증**하면 한 명의 실수가 드러나고
  평균적으로 오차가 줄어든다. 시험 답안을 4명이 맞춰보는 것과 같다.

- **우리 표에서의 역할**: 마커 기반 멀티카메라의 **현재 최강(SOTA)**. 단, 이들도 FK는 100% 믿는다.

## 6. Calib3R (2025) — "보드도 없이, AI가 장면을 3D로 복원해서"

가장 최신 흐름. 보드나 마커를 **아예 안 쓴다.**

- 카메라로 그냥 **주변 풍경**을 찍는다.
- 수백만 장의 사진으로 미리 학습된 AI(3D 파운데이션 모델)가, 여러 장의 사진에서
  **장면의 3D 구조**를 복원한다. (사람이 사진 몇 장 보고 방의 구조를 머릿속에 그리는 것처럼)
- 다만 AI가 복원한 3D는 **크기를 모른다** — 방 미니어처인지 실제 크기인지 사진만으론 모른다.
  여기서 로봇이 등장: "내가 정확히 10cm 움직였어"(FK)라는 정보로 복원된 3D의 **실제 크기를
  맞추고**, 동시에 카메라들의 위치를 로봇 기준으로 정렬한다.

마커를 만들고 붙이는 수고가 없다는 게 장점, 대신 마커만큼 정밀하기 어렵다는 게 통념이다.
**우리가 이것보다 정확하면 "왜 아직 마커를 쓰는가"라는 질문에 실험으로 답하는 셈이 된다.**

- **우리 표에서의 역할**: "보드 없는 AI 방식" 진영의 최신 대표.

---

## 7. 그래서 우리(ours-B)는 뭐가 다른가 — 한 문단

위 여섯을 "FK를 얼마나 믿는가"로 줄 세우면: Shah·Tabb·Allegro는 **100% 믿고**(FK 오차가
답에 그대로 박힘), Tsai·Daniilidis는 움직임 비교에만 쓰며, Calib3R는 크기 맞추기에만 쓴다.
우리는 **"적당히 믿는다"**: FK를 부드러운 닻(soft anchor)처럼 걸어서 — 답이 FK에서 너무
멀어지면 살짝 끌어당기되, 카메라 관측이 강하게 반대하면 관측을 따른다. 당기는 세기(λ)도
사람이 정하지 않고 데이터로 고른다. 그리고 하나 더: 캘리브가 끝난 뒤에도 남아 있는
**"위치에 따라 일정하게 반복되는 오차 패턴"** 을 발견해서(예: 작업대 왼쪽에서는 항상 2mm
왼쪽으로 틀림), 간단한 직선 공식으로 학습해 **미리 빼준다**(잔차보정). 요약: **여섯 방법 중
어느 것도 하지 않는 두 가지 — FK를 유연하게 믿기 + 반복되는 오차 지우기 — 가 우리의 기여다.**

## 8. 비교는 실제 로봇에서만 한다 (확정) — 정답 없이 어떻게 채점하나

시뮬레이션은 정답을 알지만, 실제 세계에는 "진짜 정답표"가 없다. 대신 **정확히 알 수 있는
값 4가지**로 채점한다:

| 채점 방식 | 비유 | 왜 믿을 수 있나 |
|---|---|---|
| **숨겨둔 문제 (held-out)** | 문제집으로 공부시키고 **안 보여준 문제**로 시험 | 답을 맞추려면 진짜 실력(일반화)이 필요 — 외운 걸로는 안 됨 |
| **자로 잰 실제 치수** | 카메라들이 복원한 큐브의 한 변 길이 vs **캘리퍼로 잰 실제 길이** | 물리적 실측값 = 진짜 정답 |
| **정확히 아는 이동량** | 로봇에게 "정확히 100.0mm 직진" 시키고, 카메라가 잰 이동량과 비교 | 모터 센서의 **상대 이동**은 매우 정확함 |
| **사진과의 어긋남 (픽셀)** | 답을 사진 위에 겹쳐 그려 몇 픽셀 어긋나나 | 사진에 찍힌 코너 위치 자체가 기준 |

여섯 방법 + 우리 방법 전부를 **같은 날, 같은 카메라, 같은 데이터**로 돌리고
위 네 가지로 채점한 표가 논문의 메인 표(Table I)가 된다.

---

*연관 문서: 표 골격은 [PAPER_TABLES.md](PAPER_TABLES.md), 코드·적용성 분석은
[PAPER_BASELINES.md](PAPER_BASELINES.md), 수식 버전은 Simul_test/FK_METHODS_FORMULATION.md.*
